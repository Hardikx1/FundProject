import 'vuetify/styles';
import { createVuetify } from 'vuetify';
import { VContainer, VRow, VCol, VCard, VCardTitle, VCardText, VForm, VTextField, VBtn, VDivider, VCheckbox } from 'vuetify/components';

export default createVuetify({
  components: {
    VContainer, VRow, VCol, VCard, VCardTitle, VCardText, VForm, VTextField, VBtn, VDivider, VCheckbox
  },
});
